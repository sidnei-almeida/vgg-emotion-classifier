# cnn-emotion-classifier
 A deep learning project for facial emotion recognition using a Convolutional Neural Network (CNN). This model is trained on a preprocessed dataset from FER2013 and RAFDB to classify faces into seven emotions. Built with TensorFlow and Keras.
