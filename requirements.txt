# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core dependencies (compatible with Python 3.11-3.13)
numpy>=1.24.0
pandas>=2.0.0
pillow>=9.0.0

# Machine Learning & Computer Vision (CPU Optimized)
# ✅ TensorFlow CPU-only - NO CUDA
tensorflow-cpu>=2.13.0,<2.21.0
# ⚠️ Specific Keras version for VGG16 model compatibility
# The model was trained on Colab with Keras 3.10.0
keras==3.10.0

# OpenCV headless for face detection
opencv-python-headless>=4.8.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Note: This file contains all project dependencies
# The VGG16 model requires exact Keras 3.10.0 to work correctly
